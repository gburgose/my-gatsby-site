import * as React from "react";

const FakeDefault: React.FC = () => {
  return (
    <div className="fake fake--default">
      {/* Componente en blanco */}
    </div>
  );
};

export default FakeDefault;